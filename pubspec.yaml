name: mindmochi
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0

  http: ^0.13.6  # Ensure you have the latest version


dev_dependencies:
  flutter_test:
    sdk: flutter
  url_launcher: ^6.2.5

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/